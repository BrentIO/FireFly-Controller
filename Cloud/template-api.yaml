AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: HTTP API Gateway with custom domain and ACM certificate

Parameters:
  ApiDomainName:
    Type: String
    Description: The custom domain for the API (e.g., api.p5software.com)

  CertificateDomainName:
    Type: String
    Description: The domain name for the ACM certificate (e.g., *.p5software.com)

  HostedZoneId:
    Type: String
    Description: Route53 Hosted Zone ID for p5software.com

Resources:

  ApiCertificate:
    Type: AWS::CertificateManager::Certificate
    Properties:
      DomainName: !Ref CertificateDomainName
      ValidationMethod: DNS
      DomainValidationOptions:
        - DomainName: !Ref CertificateDomainName
          HostedZoneId: !Ref HostedZoneId

  ApiGateway:
    Type: AWS::Serverless::HttpApi
    Properties:
      StageName: $default
      DisableExecuteApiEndpoint: true
      DefinitionBody:
        openapi: "3.0.1"
        info:
          title: "FireFly"
        paths:
          /health:
            get:
              responses:
                "200":
                  description: OK

  ApiCustomDomain:
    Type: AWS::ApiGatewayV2::DomainName
    Properties:
      DomainName: !Ref ApiDomainName
      DomainNameConfigurations:
        - CertificateArn: !Ref ApiCertificate
          EndpointType: REGIONAL

  ApiBasePathMapping:
    Type: AWS::ApiGatewayV2::ApiMapping
    Properties:
      ApiId: !Ref ApiGateway
      DomainName: !Ref ApiCustomDomain
      Stage: '$default'

  ApiDomainAliasRecord:
    Type: AWS::Route53::RecordSet
    Properties:
      HostedZoneId: !Ref HostedZoneId
      Name: !Ref ApiDomainName
      Type: A
      AliasTarget:
        DNSName: !GetAtt ApiCustomDomain.RegionalDomainName
        HostedZoneId: !GetAtt ApiCustomDomain.RegionalHostedZoneId

Outputs:
  ApiUrl:
    Value: !Sub "https://${ApiDomainName}"
    Description: Custom API URL
